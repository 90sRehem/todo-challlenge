import { styled } from "../../lib/stitches";

export const Container = styled("header", {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",

    height: 200,
    width: "100vw",

    background: "$gray-700"
});