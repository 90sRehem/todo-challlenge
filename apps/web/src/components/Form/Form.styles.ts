import { styled } from "@stitches/react";

export const Form = styled("form", {
    marginTop: -25,
    display: 'flex',
    flexWrap: 'wrap',
    flexDirection: 'row',
    alignItems: 'center',
    padding: 0,
    gap: 8,
    minWidth: 736,
    minHeight: 54,
});